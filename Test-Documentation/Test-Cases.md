### [TC-001] Поиск существующего города (Positive)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** High

**Критичность (Severity):** Major

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

---

#### Шаги воспроизведения (Test Steps)

| # | Действие (Step Description)                | Ожидаемый результат (Expected Result)                                                                                                                 |
| - | ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | Ввести строку `Kyiv` в поле поиска города. | Введённое значение корректно и без искажений отображается в поле ввода.                                                                               |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | 1. Происходит успешная обработка поискового запроса.<br>2. Город `Kyiv` успешно найден.<br>3. Отображается информация о погоде для выбранного города. |

---

#### Пост-условия (Postconditions)

Результаты поиска сохраняются на экране до ввода следующего запроса.

#### Данные для тестирования (Test Data)

**Valid City Name:** `Kyiv`

#### Фактический результат (Actual Result)

Город `Kyiv` успешно найден. Отображается информация о погоде для города `Kyiv`.

**Статус (Status):** PASS

### [TC-002] Поиск несуществующего города (Negative)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Medium

**Критичность (Severity):** Medium

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска пустое и готово к вводу.

---

#### Шаги воспроизведения (Test Steps)

| # | Действие (Step Description) | Ожидаемый результат (Expected Result) |
| --- | --- | --- |
| 1 | Ввести строку `Xyzqwerty123` в поле поиска города. | Символы корректно отображаются в строке поиска. Введённое значение корректно отображается в поле поиска. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Город не найден, пользователю отображается соответствующее сообщение. |
| 3 | Проверить состояние интерфейса. | 1. Отображается понятное пользователю сообщение об ошибке (например, *"Город не найден"*).<br>

<br>2. Данные предыдущего выбранного города (если он был) не искажаются.<br>

<br>3. UI приложения остается стабильным (нет падений, бесконечного лоадера или сырых системных ошибок/stack trace). |

---

#### Пост-условия (Postconditions)

* Поле поиска остаётся доступным для ввода нового корректного запроса.

#### Данные для тестирования (Test Data)

* **Invalid City Name:** `Xyzqwerty123`

#### Фактический результат (Actual Result)

При вводе `Xyzqwerty123` отображается уведомление `City not found`. Данные предыдущего города остаются без изменений. Интерфейс работает стабильно, бесконечная загрузка отсутствует.

**Статус (Status):** PASS

### [TC-003] Поиск города с несколькими словами в названии (Positive)

**Модуль:** City Search / Weather Dashboard  
Приоритет (Priority): High  

**Предусловия (Preconditions):**
1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

---

#### Шаги воспроизведения (Test Steps)

| # | Действие (Step Description) | Ожидаемый результат (Expected Result) |
|---|-----------------------------|---------------------------------------|
| 1 | Ввести строку `New York` в поле поиска города. | Введённое значение корректно и без искажений отображается в поле ввода. | 2. В выпадающем списке (если есть) появляется подсказка New York. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | 1. Происходит успешная отправка запроса.<br>2. Отображается страница/карточка с данными погоды для города New York.<br> |

---

#### Пост-условия (Postconditions)
Результаты поиска сохраняются на экране до ввода следующего запроса.

#### Данные для тестирования (Test Data)
Valid City Name (составное): New York (альтернативные варианты: Rio de Janeiro, San Francisco)

#### Фактический результат (Actual Result)

При вводе `New York` отображается подсказка `New York, New York, United States`. После нажатия `Enter` найден город `New York`, отображается информация о погоде для него.

**Статус (Status):** PASS


### [TC-004] Поиск города по частичному названию (Positive)

**Модуль:** City Search / Weather Dashboard  
Приоритет (Priority): Medium  

**Предусловия (Preconditions):**
1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

---

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Ввести подстроку `Kyi` в поле поиска. | Введенный текст отображается в поле. Появляется список подсказок, в котором присутствует `Kyiv`. |
| 2 | Выбрать `Kyiv` из списка или нажать `Enter`. | Поле автоматически заполняется полным названием. Загружаются данные погоды для `Kyiv`. |

---



#### Пост-условия (Postconditions)
Результаты поиска сохраняются на экране до ввода следующего запроса.

**Данные для тестирования:** `Kyi`

#### Фактический результат (Actual Result)

При вводе `Kyi` отображается подсказка `Kyiv`. При выборе подсказки поле заполняется значением `Kyiv` и отображается информация о погоде для города `Kyiv`. При нажатии `Enter` также выполняется поиск, в результате отображается город `Kyiv` и его погодные данные.

**Статус (Status):** PASS


### [TC-005] Поиск города в любом регистре (Positive)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Medium

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)** | **Ожидаемый результат (Expected Result)** |
|---|---|---|
| 1 | Ввести строку `kYiV` в поле поиска города. | Введённое значение корректно отображается в поле ввода. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Запрос успешно обрабатывается независимо от регистра символов. Отображается информация о погоде для города Kyiv. |

**Пост-условия (Postconditions):**

Результаты поиска сохраняются на экране до ввода следующего запроса.

**Данные для тестирования (Test Data):**

Valid City Name (mixed case): `kYiV`


#### Фактический результат (Actual Result)

При вводе `kYiV` и нажатии `Enter` успешно найден город `Kyiv`. Отображается информация о погоде для города `Kyiv`.

**Статус (Status):** PASS

### [TC-006] Пустой поисковый запрос (Negative)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Medium

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)** | **Ожидаемый результат (Expected Result)** |
|---|---|---|
| 1 | Оставить поле поиска города пустым. | Поле остаётся пустым. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Поиск не выполняется. Приложение корректно обрабатывает пустой поисковый запрос и отображает соответствующее сообщение или не выполняет действие. UI остаётся стабильным. |

**Пост-условия (Postconditions):**

Поле поиска остаётся доступным для ввода нового запроса.

**Данные для тестирования (Test Data):**

Empty string: `""`

#### Фактический результат (Actual Result)

При пустом поисковом запросе и нажатии `Enter` поиск не выполняется. Приложение не выполняет никаких действий, интерфейс остаётся стабильным.

**Статус (Status):** PASS

**Примечание:** Отсутствует визуальная обратная связь для пользователя о том, что пустой запрос не был обработан. Зафиксировано как UX-наблюдение.

### [TC-007] Поиск со специальными символами (Negative)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** High

**Критичность (Severity):** Medium

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)** | **Ожидаемый результат (Expected Result)** |
|---|---|---|
| 1 | Ввести строку `@@$$!!` в поле поиска города. | Введённые символы корректно отображаются в поле ввода. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Приложение корректно обрабатывает запрос со специальными символами. Город не найден, отображается соответствующее сообщение. Приложение не приводит к сбою или некорректному отображению интерфейса. |

**Пост-условия (Postconditions):**

Поле поиска остаётся доступным для ввода нового запроса.

**Данные для тестирования (Test Data):**

Special characters: `@@$$!!`

#### Фактический результат (Actual Result)

При вводе `@@$$!!` значение корректно отображается в поле поиска. После нажатия `Enter` поиск не находит город, отображается сообщение `City not found`. Интерфейс остаётся стабильным, ошибок отображения не обнаружено.

**Статус (Status):** PASS

### [TC-008] Пробелы в начале и в конце поискового запроса (Positive)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Medium

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)** | **Ожидаемый результат (Expected Result)** |
|---|---|---|
| 1 | Ввести строку `  Kyiv  ` с пробелами в начале и в конце. | Введённое значение отображается в поле ввода. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Приложение корректно обрабатывает пробелы в начале и в конце запроса. Отображается информация о погоде для города Kyiv. |

**Пост-условия (Postconditions):**

Результаты поиска сохраняются на экране.

**Данные для тестирования (Test Data):**

String with spaces: `  Kyiv  `

#### Фактический результат (Actual Result)

При вводе `  Kyiv  ` и нажатии `Enter` успешно найден город `Kyiv`. Отображается информация о погоде для города `Kyiv`. Пробелы в начале и конце поискового запроса не препятствуют выполнению поиска.

**Статус (Status):** PASS

**Примечание:** Введённые пробелы визуально отображаются в поле поиска, однако поиск корректно обрабатывает запрос.



### [TC-009] Поиск города с опечаткой (Negative)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Low

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)** | **Ожидаемый результат (Expected Result)** |
|---|---|---|
| 1 | Ввести строку `Kyv` с пропущенной буквой в поле поиска города. | Введённое значение корректно отображается в поле ввода. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Приложение корректно обрабатывает поисковый запрос с опечаткой. Результат поиска соответствует требованиям приложения. |

**Пост-условия (Postconditions):**

Поле поиска остаётся доступным для ввода нового запроса.

**Данные для тестирования (Test Data):**

Typo City Name: `Kyv`


#### Фактический результат (Actual Result)

При вводе `Kyv` и нажатии `Enter` город не найден. В интерфейсе отображается сообщение `City not found`. Приложение работает стабильно, ошибок интерфейса не обнаружено.

**Статус (Status):** PASS

**Примечание:** Автоматическое исправление опечатки или предложение варианта `Kyiv` отсутствует. Это не считается дефектом, так как требование о поддержке поиска с опечатками не определено.


### [TC-010] Поиск города при неправильной раскладке клавиатуры (Negative)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Low

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)** | **Ожидаемый результат (Expected Result)** |
|---|---|---|
| 1 | Ввести строку `Лншм` в поле поиска города. | Введённое значение корректно отображается в поле ввода. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Приложение корректно обрабатывает запрос, введённый в неправильной раскладке. |

**Пост-условия (Postconditions):**

Поле поиска остаётся доступным для ввода нового запроса. Данные предыдущего города не искажаются.

**Данные для тестирования (Test Data):**

Wrong layout string: `Лншм`

#### Фактический результат (Actual Result)

При вводе `Лншм` и нажатии `Enter` город не найден. В интерфейсе отображается сообщение `City not found`. Данные предыдущего города остаются без изменений, интерфейс работает стабильно.

**Статус (Status):** PASS

**Примечание:** Автоматическое преобразование текста, введённого в неправильной раскладке клавиатуры, отсутствует. Зафиксировано как UX-наблюдение.


### [TC-011] Поиск города на разных языках (Positive)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Medium

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)** | **Ожидаемый результат (Expected Result)** |
|---|---|---|
| 1 | Ввести название города на кириллице `Київ` в поле поиска. | Введённое значение корректно отображается в поле ввода. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Поиск корректно обрабатывается. Отображается информация о погоде для соответствующего города. |

**Пост-условия (Postconditions):**

Результаты поиска сохраняются на экране.

**Данные для тестирования (Test Data):**

Localized City Name: `Київ`


#### Фактический результат (Actual Result)

При вводе `Київ` и нажатии `Enter` город не найден. В интерфейсе отображается сообщение `City not found`. Предыдущие данные города остаются без изменений.

При этом русскоязычное написание `Киев` успешно обрабатывается на русском интерфейсе.

**Статус (Status):** FAIL

**Баг:** Bug #3 — Поиск города `Київ` не работает


### [TC-012] Поиск при вводе названия на двух языках одновременно (Negative)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Low

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)** | **Ожидаемый результат (Expected Result)** |
|---|---|---|
| 1 | Ввести строку `Kиїв`, где `K` — латинский символ, а `иїв` — кириллические символы. | Введённое значение корректно отображается в поле ввода. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Приложение корректно обрабатывает смешанный поисковый запрос. UI остаётся стабильным. |

**Пост-условия (Postconditions):**

Поле поиска остаётся доступным для ввода нового запроса.

**Данные для тестирования (Test Data):**

Mixed language string: `Kиїв`

#### Фактический результат (Actual Result)

При вводе смешанного поискового запроса `Kиїв` и нажатии `Enter` город не найден. В интерфейсе отображается сообщение `City not found`. Предыдущие данные города остаются без изменений, интерфейс работает стабильно.

**Статус (Status):** PASS

**Примечание:** Автоматическая нормализация смешанного латинского и кириллического написания отсутствует. Зафиксировано как UX-наблюдение.

### [TC-013] Поиск с цифрами (Negative)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Medium

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)** | **Ожидаемый результат (Expected Result)** |
|---|---|---|
| 1 | Ввести строку `Kyiv123` в поле поиска города. | Введённое значение корректно отображается в поле ввода. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Приложение корректно обрабатывает поисковый запрос, содержащий цифры. Город не найден, отображается соответствующее сообщение. |

**Пост-условия (Postconditions):**

Поле поиска остаётся доступным для ввода нового запроса.

**Данные для тестирования (Test Data):**

String with numbers: `Kyiv123`


#### Фактический результат (Actual Result)

При вводе `Kyiv123` и нажатии `Enter` город не найден. В интерфейсе отображается сообщение `City not found`. Приложение работает стабильно.

**Статус (Status):** PASS

### [TC-014] Поиск с эмодзи (Negative)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Low

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)** | **Ожидаемый результат (Expected Result)** |
|---|---|---|
| 1 | Ввести строку `Kyiv 🇺🇦` в поле поиска города. | Введённое значение, включая эмодзи, корректно отображается в поле ввода. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Приложение корректно обрабатывает поисковый запрос с эмодзи. Отображается соответствующий результат поиска, приложение остаётся стабильным. |

**Пост-условия (Postconditions):**

Поле поиска остаётся доступным для ввода нового запроса.

**Данные для тестирования (Test Data):**

String with emoji: `Kyiv 🇺🇦`

#### Фактический результат (Actual Result)

При вводе `Kyiv 🇺🇦` и нажатии `Enter` поисковый запрос обрабатывается корректно. Город не найден, в интерфейсе отображается сообщение `City not found`. Интерфейс остаётся стабильным.

**Статус (Status):** PASS



### [TC-015] Поисковый запрос, состоящий только из пробелов (Negative)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Medium

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)** | **Ожидаемый результат (Expected Result)** |
|---|---|---|
| 1 | Ввести 5 пробелов в поле поиска города. | Введённые пробелы обрабатываются корректно. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Поиск не выполняется как поиск города. Приложение корректно обрабатывает запрос, состоящий только из пробелов. |

**Пост-условия (Postconditions):**

Поле поиска остаётся доступным для ввода нового запроса.

**Данные для тестирования (Test Data):**

Spaces only: `     `


#### Фактический результат (Actual Result)

При вводе пяти пробелов и нажатии `Enter` поиск не выполняется. Приложение не выполняет никаких действий, интерфейс остаётся стабильным.

**Статус (Status):** PASS

**Примечание:** Поисковый запрос, состоящий только из пробелов, фактически обрабатывается как пустой запрос.

### [TC-016] Проверка минимального количества символов для поиска (Boundary Value)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Medium

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)**                | **Ожидаемый результат (Expected Result)**                 |
| ----- | ---------------------------------------------- | --------------------------------------------------------- |
| 1     | Ввести `K` (1 символ) в поле поиска города.    | Подсказки не отображаются, поиск по Enter не выполняется. |
| 2     | Ввести `Ky` (2 символа) в поле поиска города.  | Подсказки начинают отображаться, поиск по Enter работает. |
| 3     | Ввести `Kyi` (3 символа) в поле поиска города. | Подсказки отображаются, поиск по Enter работает.          |

**Пост-условия (Postconditions):**

Поле поиска остаётся доступным для ввода нового запроса.

**Данные для тестирования (Test Data):**

* `K` — 1 символ (`min - 1`)
* `Ky` — 2 символа (`min`)
* `Kyi` — 3 символа (`min + 1`)

#### Фактический результат (Actual Result)

**1 символ (`K`):** подсказки не отображаются. При нажатии `Enter` поиск не выполняется.

**2 символа (`Ky`):** при наличии результатов отображаются поисковые подсказки. При нажатии `Enter` поиск выполняется, однако первый результат может отличаться от `Kyiv` и зависит от возвращаемых результатов поиска.

**3 символа (`Kyi`):** отображаются поисковые подсказки. При нажатии `Enter` успешно находится город `Kyiv`.

**Статус (Status):** PASS

**Примечание:** Минимальный порог обработки поискового запроса составляет 2 символа. Для запроса из 2 символов конкретный найденный город зависит от результатов поиска и не должен автоматически считаться `Kyiv`.


### [TC-017] Проверка максимальной допустимой длины поискового запроса (Boundary Value)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Medium

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается, пустое и готово к вводу.
3. Максимальная допустимая длина поискового запроса не определена в требованиях.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)**                                                                   | **Ожидаемый результат (Expected Result)**                                                                                             |
| ----- | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| 1     |Ввести в поле поиска строку из 1000 символов. | Все введённые символы принимаются полем. Атрибут maxlength отсутствует.|
| 2     | Проверить возможность продолжить ввод символов.| Поле продолжает принимать дополнительные символы, ограничение максимальной длины не установлено на уровне поля. |

**Пост-условия (Postconditions):**

Поле поиска остаётся доступным для ввода нового запроса.

**Данные для тестирования (Test Data):**

Long search query: A × 1000

#### Фактический результат (Actual Result)

Поле поиска принимает поисковый запрос длиной 1000 символов. Возможность продолжить ввод после 1000 символов сохраняется, ограничение длины на уровне поля отсутствует.

При отправке запроса приложение обрабатывает введённое значение.

**Статус (Status):** FAIL

**Примечание:** Максимальная допустимая длина поискового запроса не определена в требованиях. Отсутствие ограничения зафиксировано как потенциальный gap в требованиях, но не как дефект.

### [TC-018] Корректное отображение данных найденного города (UI)

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Medium

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается и готово к вводу.

#### Шаги воспроизведения (Test Steps)

| # | Действие (Step Description) | Ожидаемый результат (Expected Result) |
|---|---|---|
| 1 | Ввести название существующего города `Kyiv` в поле поиска и нажать `Enter`. | Выполняется поиск города и отображается блок с информацией о погоде. |
| 2 | Проверить отображаемое название города. | Название города отображается корректно и соответствует найденному городу (`Kyiv`). |
| 3 | Проверить содержимое блока с погодой. | Информация о погоде отображается корректно, элементы блока не перекрывают друг друга и не выходят за границы контейнера. |

#### Пост-условия (Postconditions)

Карточка с погодой для найденного города остаётся отображаться на экране.

#### Данные для тестирования (Test Data)

**Valid City Name:** `Kyiv`

#### Фактический результат (Actual Result)

После ввода `Kyiv` и нажатия `Enter` отображается информация о погоде для города `Kyiv`. Название города отображается корректно, элементы погодного блока отображаются на экране.

**Статус (Status):** PASS

**Примечание:** В рамках данного тест-кейса проверялось корректное отображение города и погодного блока через UI. Дополнительные замечания по API и поведению загрузки рассматриваются отдельно.



### [TC-019] Отображение погоды именно выбранного города

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** High

**Критичность (Severity):** Major

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается и готово к вводу.

#### Шаги воспроизведения (Test Steps)

| # | Действие (Step Description) | Ожидаемый результат (Expected Result) |
|---|---|---|
| 1 | Ввести название существующего города `Kyiv` в поле поиска и нажать `Enter`. | Выполняется поиск города и отображается информация о погоде. |
| 2 | Проверить название города в отображаемом блоке с погодой. | Отображаемый город соответствует введённому городу `Kyiv`. |
| 3 | Проверить данные о погоде в отображаемом блоке. | Данные о погоде относятся к выбранному городу `Kyiv` и отображаются без подмены данными другого города. |

#### Пост-условия (Postconditions)

Информация о погоде для выбранного города остаётся отображаться на экране.

#### Данные для тестирования (Test Data)

**Valid City Name:** `Kyiv`


#### Фактический результат (Actual Result)

После поиска города `Kyiv` отображается информация о погоде именно для выбранного города. Заголовок города отображается как `Kyiv`. Временная зона отображается как `Europe/Kyiv`, а время восхода и заката соответствует местному времени города.

Дополнительно данные были сверены с данными API для координат `Kyiv`.

**Статус (Status):** PASS

### [TC-020] Отображение сообщения об ошибке при поиске несуществующего города

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** Medium

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается и готово к вводу.

#### Шаги воспроизведения (Test Steps)

| # | Действие (Step Description) | Ожидаемый результат (Expected Result) |
|---|---|---|
| 1 | Ввести несуществующее название города `Xyzqwerty123` в поле поиска. | Введённое значение корректно отображается в поле поиска. |
| 2 | Нажать кнопку «Поиск» или клавишу `Enter`. | Поиск выполняется, и в интерфейсе отображается сообщение об ошибке `City not found`. |
| 3 | Проверить отображение сообщения об ошибке. | Сообщение `City not found` отображается в отдельном уведомлении и не нарушает отображение интерфейса. |

#### Пост-условия (Postconditions)

Поле поиска остаётся доступным для ввода нового запроса.

#### Данные для тестирования (Test Data)

**Invalid City Name:** `Xyzqwerty123`


#### Фактический результат (Actual Result)

При поиске несуществующего города отображается уведомление `City not found` в нижней центральной части интерфейса. Уведомление не нарушает работу интерфейса и автоматически исчезает через короткое время.

**Статус (Status):** PASS

**Примечание:** Сообщение об ошибке отображается корректно. Продолжительность отображения уведомления составляет примерно 2,2 секунды.

### [TC-021] Предыдущий город не подменяется некорректными данными после неудачного поиска

**Модуль:** City Search / Weather Dashboard

**Приоритет (Priority):** High

**Критичность (Severity):** Major

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Поле поиска отображается и готово к вводу.

#### Шаги воспроизведения (Test Steps)

| # | Действие (Step Description) | Ожидаемый результат (Expected Result) |
|---|---|---|
| 1 | Ввести существующий город `Kyiv` в поле поиска и нажать `Enter`. | Отображается информация о погоде для города `Kyiv`. |
| 2 | Проверить отображаемый город и его погодные данные. | На экране отображается информация именно для `Kyiv`. |
| 3 | Заменить значение в поле поиска на несуществующий город `Xyzqwerty123` и нажать `Enter`. | Отображается сообщение об ошибке `City not found`. |
| 4 | Проверить блок с погодой после неудачного поиска. | Данные предыдущего города не подменяются некорректными данными. Интерфейс остаётся в корректном состоянии. |

#### Пост-условия (Postconditions)

Приложение отображает корректное состояние после неудачного поиска и позволяет выполнить новый поисковый запрос.

#### Данные для тестирования (Test Data)

**Valid City Name:** `Kyiv`

**Invalid City Name:** `Xyzqwerty123`

#### Фактический результат (Actual Result)

После успешного поиска города `Kyiv` выполнен поиск несуществующего города `Xyzqwerty123`.

Отображается уведомление `City not found`. Данные ранее выбранного города `Kyiv` остаются без изменений. Погодная информация не подменяется некорректными данными. Сохранённый город `Kyiv` также остаётся в приложении.

**Статус (Status):** PASS

### [TC-023] Проверка локализации элементов интерфейса (Positive)

**Модуль:** Localization / Weather Dashboard

**Приоритет (Priority):** Medium

**Критичность (Severity):** Minor

**Предусловия (Preconditions):**

1. Главная страница приложения открыта.
2. Интерфейс приложения доступен на русском, английском и украинском языках.
3. Переключатель языка доступен пользователю.

**Шаги воспроизведения (Test Steps)**

| **#** | **Действие (Step Description)**                 | **Ожидаемый результат (Expected Result)**                                                          |
| ----- | ----------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| 1     | Открыть приложение с русским языком интерфейса. | Все текстовые элементы интерфейса отображаются на русском языке.                                   |
| 2     | Переключить язык интерфейса на английский.      | Все текстовые элементы интерфейса, включая кнопки **3** и **7**, отображаются на английском языке. |
| 3     | Переключить язык интерфейса на украинский.      | Все текстовые элементы интерфейса, включая кнопки **3** и **7**, отображаются на украинском языке. |
| 4     | Переключить язык обратно на русский.            | Все текстовые элементы интерфейса отображаются на русском языке.                                   |

**Пост-условия (Postconditions):**

Выбранный язык интерфейса сохраняется и применяется ко всем локализуемым элементам приложения.

**Данные для тестирования (Test Data):**

Languages:

* Russian
* English
* Ukrainian

UI Elements:

* Button 3
* Button 7

#### Фактический результат (Actual Result)

При переключении интерфейса на английский или украинский язык текст кнопок **3** и **7** остаётся на русском языке.

Остальные элементы интерфейса переводятся в соответствии с выбранным языком.

**Статус (Status):** FAIL

**Связанный баг:** BUG-003 — кнопки 3 и 7 не переводятся при смене языка.

### [TC-024] Проверка точности данных: облачность, видимость и UV Index

**Модуль:** Weather Dashboard

**Приоритет (Priority):** High

**Критичность (Severity):** Critical

**Предусловия (Preconditions):**

1. Приложение Weather Forecast открыто.
2. Выбран существующий город с доступными почасовыми погодными данными.
3. Текущее время известно и используется для определения текущего часа.

**Шаги воспроизведения (Test Steps)**

| # | Действие (Step Description)                                                                     | Ожидаемый результат (Expected Result)                                                                   |
| - | ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| 1 | Выбрать город **London**.                                                                       | Погодные данные для London отображаются корректно.                                                      |
| 2 | Зафиксировать текущие значения **Cloudiness, Visibility и UV Index**, отображаемые приложением. | Значения отображаются для текущего часа.                                                                |
| 3 | Открыть данные Open-Meteo для того же города.                                                   | Доступны почасовые погодные данные.                                                                     |
| 4 | Найти значения **Cloudiness, Visibility и UV Index** для текущего часа.                         | Значения для текущего часа доступны для сравнения.                                                      |
| 5 | Сравнить значения на сайте со значениями Open-Meteo за текущий час.                             | Значения на сайте должны соответствовать данным Open-Meteo для текущего часа.                           |
| 6 | Дополнительно сравнить значения на сайте со значениями Open-Meteo за **00:00 текущего дня**.    | Значения на сайте не должны соответствовать данным за 00:00, если текущее время отличается от полуночи. |

**Тестовые данные (Test Data):**

* **City:** London
* **Test time:** 14:15
* **Data source:** Open-Meteo
* **Parameters:**

  * Cloudiness
  * Visibility
  * UV Index

#### Фактический результат (Actual Result)

При проверке в **14:15** значения, отображаемые приложением, соответствуют данным Open-Meteo за **00:00**, а не данным за текущий час.

Пример:

| Parameter  | Current hour — Open-Meteo | Application |
| ---------- | ------------------------: | ----------: |
| Cloudiness |                      100% |          0% |
| Visibility |                   19.5 km |     27.7 km |
| UV Index   |                       2.2 |         0.0 |

Таким образом, приложение отображает погодные показатели, соответствующие данным за **00:00**, вместо текущего часа.

**Статус (Status):** ❌ FAIL

**Связанный баг:** BUG-004 — Погодные показатели отображаются за 00:00 вместо текущего часа.


